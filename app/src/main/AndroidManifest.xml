<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Only permission this app needs: talking to the API / downloading the runtime.
         No storage permission, no root, nothing else. Everything lives in this
         app's private folder (filesDir), which needs zero permission grants. -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:label="Claude Pocket"
        android:icon="@mipmap/ic_launcher"
        android:theme="@style/Theme.ClaudePocket"
        android:allowBackup="false">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>
</manifest>
