[gd_scene load_steps=2 format=3]

; survival_shipping_main.gd extends the verified multi-LOD, collision, directional
; streaming, safe cache, biome palette, and chunk-local vegetation stack.
; It adds survival-only inventory, drops, placement costs, persistence, and HUD.
[ext_resource type="Script" path="res://src/main/survival_shipping_main.gd" id="1_main"]

[node name="Main" type="Node3D"]
script = ExtResource("1_main")
